import { Routes } from '@angular/router';
import { AppointmentsPageComponent } from './appointments-page.component';

export default [
  { path: '', component: AppointmentsPageComponent, title: 'Poshan · Appointments' },
] satisfies Routes;
