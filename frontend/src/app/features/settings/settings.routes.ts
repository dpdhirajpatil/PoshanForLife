import { Routes } from '@angular/router';
import { SettingsPageComponent } from './settings-page.component';

export default [
  { path: '', component: SettingsPageComponent, title: 'Poshan · Settings' },
] satisfies Routes;
