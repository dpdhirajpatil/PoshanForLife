<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only: permits cleartext HTTP to the local dev backend. This file
  lives under src/debug/, so it's never packaged into the release build —
  release stays HTTPS-only against api.poshanforlife.com by Android's
  platform default.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
