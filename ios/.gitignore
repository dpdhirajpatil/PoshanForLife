# Per-machine overrides (LAN IP, signing team) — never committed.
Config/Local.xcconfig

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
.DS_Store
PoshanForLife.xcodeproj/

# Generated by xcodegen from project.yml's entitlements.properties on every
# `generate` — same reasoning as the .xcodeproj line above. Source of truth
# is project.yml; edits made directly to the .entitlements file are silently
# overwritten the next time anyone runs `xcodegen generate`.
PoshanForLife/PoshanForLife.entitlements
