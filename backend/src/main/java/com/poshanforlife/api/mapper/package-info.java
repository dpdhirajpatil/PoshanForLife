/**
 * MapStruct mappers (entity &lt;-&gt; DTO). Feature prompts add mappers here.
 * The default component model is set to "spring" via a compiler arg in pom.xml,
 * so {@code @Mapper} interfaces are injectable beans without extra attributes.
 */
package com.poshanforlife.api.mapper;
